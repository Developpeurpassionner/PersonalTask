import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ibrahim.personaltask',
  appName: 'PersonnalTask',
  webDir: 'public'
};

export default config;
